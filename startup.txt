=======================================
  VEND88 Dashboard - Startup Commands
=======================================

--- FIRST TIME SETUP (run once) ---

1. Install frontend dependencies:
   npm install

2. Create backend virtual environment:
   cd backend
   python -m venv venv

3. Install backend dependencies:
   cd backend
   venv\Scripts\activate
   pip install -r requirements.txt

--- RUN THE APP (every time) ---

Terminal 1 - Backend:
   cd backend
   venv\Scripts\activate
   uvicorn main:app --reload

Terminal 2 - Frontend:
   npm start
   # API target switch (optional):
   # $env:REACT_APP_API_TARGET="official"
   # $env:REACT_APP_API_TARGET="custom"
.
--- URLs ---
Frontend:  http://localhost:3000
Backend:   http://localhost:8000
API Docs:  http://localhost:8000/docs

--- RUN MOBILE APP WITH EXPO GO ---

Terminal 3 - Mobile:
   cd mobile
   npm install
   npx expo start --lan

On iPhone (Expo Go):
   1. Install Expo Go from App Store
   2. Scan the QR code from terminal

On Android (Expo Go):
   1. Install Expo Go from Play Store
   2. Scan the QR code from terminal

--- MOBILE API CONFIG ---

1. Choose API target:
   $env:EXPO_PUBLIC_API_TARGET="official"
   # or
   $env:EXPO_PUBLIC_API_TARGET="custom"
2. If using custom backend, set custom API base URL:
   $env:EXPO_PUBLIC_CUSTOM_API_BASE_URL="http://192.168.1.55:8000/api/v1"
3. Start backend with host binding:
   uvicorn main:app --reload --host 0.0.0.0 --port 8000
4. In mobile terminal, start Expo:
   npx expo start --lan

--- EAS COMMANDS ---

From the mobile folder:
   cd mobile
   npx --yes eas-cli login

Important:
   - Run EAS commands from the mobile folder only, or use the root wrapper scripts.
   - Running `eas build` from the workspace root can pick up the root `app.json` and cause Expo project slug/projectId mismatch errors.

Preflight before using EAS build quota:
   node -v
   java -version
   npx expo-doctor
   .\android\gradlew.bat :app:assembleDebug --stacktrace
   .\android\gradlew.bat :app:assembleRelease --stacktrace

Requirements for local Android preflight:
   - Use Node 20.19.4 or newer.
   - Install JDK 17 and set JAVA_HOME before running gradlew.
   - If local Gradle assemble passes, the EAS gradlew phase is much less likely to fail on native compile issues.

Run in Expo Go:
   npx expo start

Push an OTA update:
   npx --yes eas-cli update --branch production --message "update message"

Create a cloud build:
   npx --yes eas-cli build
   # or from repo root:
   npm run eas:preview

Submit a build to the stores:
   npx --yes eas-cli submit

Note:
   - localhost does not work from phone unless using tunnel/proxy mode.
   - LAN mode is recommended first for stability and speed.
   - If LAN cannot connect, use fallback:
     npx expo start --tunnel
   - Login currently supports demo mode if backend auth endpoint is not available.



   demo@vend88.com 
   vend8800


   npx --yes eas-cli build -p android --profile preview


cd mobile
npx expo prebuild --platform ios --clean
npx expo run:ios



npx expo run:ios --device --configuration Release


--- APP STORE SCREENSHOT RESIZE (Desktop PNGs) ---

Portrait (1284 x 2778):
mkdir -p ~/Desktop/asc-ready && for f in ~/Desktop/*.png; do [ -e "$f" ] || continue; sips --resampleHeightWidth 2778 1284 "$f" --out ~/Desktop/asc-ready/"$(basename "$f")"; done

Landscape (2778 x 1284):
mkdir -p ~/Desktop/asc-ready && for f in ~/Desktop/*.png; do [ -e "$f" ] || continue; sips --resampleHeightWidth 1284 2778 "$f" --out ~/Desktop/asc-ready/"$(basename "$f")"; done

If files are in a Desktop subfolder, replace ~/Desktop/*.png with that folder path.


I’ll guide you through the exact internal iOS distribution flow for your Expo app on Windows, with the minimal commands and decisions needed so you get a standalone install link (no Expo Go, no App Store review). I’ll first check your existing EAS config so the steps match your project exactly.

Perfect. Your project is already set up for internal iOS distribution, so this is mostly an execution flow now.

I checked:

Internal build profile already exists in eas.json
iOS bundle identifier is set in app.json
Do this from D:/Github/vend88-dashboard-app/mobile.

Sign in to Expo
npx eas login
npx eas whoami
Register the iPhone(s) that should install the app
npx eas device:create
What happens:

EAS gives you a link/QR
Open it on each iPhone in Safari
Install Apple’s temporary device-registration profile
Return to browser and confirm registration is complete
Build iOS internal distribution (standalone app, no Expo Go)
npx eas build -p ios --profile preview
Install on iPhone
When build finishes, open the install link from EAS on the iPhone
Tap install
First launch trust step (one-time per certificate)
iPhone Settings > General > VPN & Device Management

EAS_SKIP_AUTO_FINGERPRINT=1 npx --yes eas-cli build -p ios --profile preview

npx --yes eas-cli device:create